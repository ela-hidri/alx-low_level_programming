#include "main.h"
/**
 * print_sign -  prints the sign of a number.
 * @c: The character to check
 *
 * Return: if greater than zero 1.
 * if equle to 0, 0 is returned
 * else , -1 returnd
 */
int print_sign(int n)
{
if (n > 0)
return (1);
else if ( n == 0)
return (0);
else 
return (-1);
}
